{
  "name": "@dep/is",
  "version": "2.0.6",
  "exports": "./src/mod.ts",
  "compilerOptions": {
    "strict": true
  },
  "imports": {
    "@/": "./src/",
    "@dep/assert": "jsr:@dep/assert@^1.3.0"
  },
  "tasks": {
    "start": "deno run --allow-net src/mod.ts",
    "build": "deno run --allow-read src/build.ts",
    "dev": "deno run --allow-env --allow-net --watch src/mod.ts",
    "check-types": "deno check src/**/*.ts",
    "lint": "deno lint",
    "fmt": "deno fmt",
    "prepublish": "deno lint && deno fmt",
    "publish": "deno task prepublish && jsr publish ./"
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  }
}
